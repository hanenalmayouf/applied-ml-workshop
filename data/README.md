# البيانات

توضع الملفات الأصلية في `data/raw/`. يفضّل parquet لأنه يحافظ على الأنواع ويقرأ أسرع. راجع `data_dictionary.md` قبل اختيار أي عمود.

ملفات `refund_issued` و`support_ticket_after_snapshot` و`next_month_orders` مزروعة عمدًا لتدريب الطالب على كشف التسرب.
